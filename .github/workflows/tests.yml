name: Run Tests

on:
  push:
    branches: [main]       # runs on every push to main
  pull_request:
    branches: [main]       # runs on every PR targeting main

jobs:
  test:
    name: Run PyTest Suite
    runs-on: ubuntu-latest  

    steps:

      # Step 1: Check out the code from the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Set up the Python version
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Step 3: Cache pip dependencies
      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # Step 4: Install dependencies
      - name: Install dependencies
        run: pip install -r requirements.txt

      # Step 5: Run the tests
      - name: Run tests
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL_NAME: ${{ vars.GEMINI_MODEL_NAME }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GROQ_WHISPER_MODEL: ${{ vars.GROQ_WHISPER_MODEL }}
          TRANSCRIBER_PROVIDER: ${{ vars.TRANSCRIBER_PROVIDER }}
          LOG_LEVEL: INFO
          LOG_FILE: logs/app.log
        run: |
          pytest --tb=short -v --exitfirst